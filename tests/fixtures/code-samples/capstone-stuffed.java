// fixture: capstone keyword dump — no class, just every capstone keyword the old regex rules counted; expected: parses + opmode-present CRITICAL, fails, score < 40
Intake Scorer Sensors Robot private DcMotor motor; private Servo servo; enum State { IDLE } average filter samples readings consecutive ElapsedTime timeout fallback park alliance mirror isRed isBlue p.of(9, 60, 0); p.of(48, 36, -45); p.of(36, 72, 0); p.of(60, 96, 90); line(start, sample).linear(start, sample); follower.update(); Scheduler.execute(); follow(follower, toPark()); sequential(race(follow(follower, toSample()), waitMs(6000))); telemetry.addData("a", 1); telemetry.addData("b", 2); telemetry.addData("c", 3); telemetry.addData("d", 4);
