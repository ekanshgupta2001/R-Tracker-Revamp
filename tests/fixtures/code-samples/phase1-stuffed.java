// fixture: phase1 keyword dump — an OpMode packed onto one line whose drive, claw and telemetry calls use names it never declares (no hardwareMap at all); expected: declared-identifiers CRITICAL, fails, score < 40
public class StuffedTeleOp extends LinearOpMode { @Override public void runOpMode() { waitForStart(); while (opModeIsActive()) { leftDrive.setPower(-gamepad1.left_stick_y); rightDrive.setPower(-gamepad1.left_stick_y + gamepad1.right_stick_x); if (gamepad1.a) claw.setPosition(1); intake.setPower(gamepad1.right_trigger); telemetry.addData("a", 1); telemetry.addData("b", 2); telemetry.update(); } } }
